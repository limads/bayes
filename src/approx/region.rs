/// Trait shared by structures that represent a bounded region on the n-dimensional real
/// vector space. Can be implemented by symmetric positive-definite covariance matrices and
/// a corresponding scalar quantile; Or by more general non-parametric formulations (such as
/// marginal or bivariate histograms).
pub trait Region {

}

/// Represents the elliptical contour of isoprobabilities in R^N.
pub struct Contour {

}
