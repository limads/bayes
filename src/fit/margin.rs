/// Defines a maximum-margin classifier. Starts from the Gram matrix
/// of a set of data points (xi, yi) (Kernel matrix, which is a similarity
/// matrix, or distance matrix, which is a dissimilarity matrix,
/// of input points). The kernel of a pair of points define some kind
/// of inner product in feature space (required to build the normal to
/// the separating hyperplane).
pub struct MarginClassifier {

}
