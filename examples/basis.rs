use bayes::distr::{Normal, Distribution};
//use bayes::basis::{ cov::*, poly::*, signal::*, cluster::* };

fn main() {
    /*let n = Normal::new(100, None, None);
    let sample = n.sample();
    let f = FFT::new(sample);
    printlnl!("{}", f.basis().nth(1) );
    println!("{}", f.coefficients().nth(2) );*/
}


